<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.frases_do_dia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icone_app"
        android:label="Frases do Dia"
        android:roundIcon="@mipmap/icone_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Frases"></activity>
        <activity android:name=".EscolhaFrases" />
        <activity android:name=".JogoEmily" />
        <activity android:name=".JogoNormal" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>